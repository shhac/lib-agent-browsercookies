version: "2"

linters:
  default: standard
  exclusions:
    rules:
      # Test files do lots of _ = thing.Close() / _ = Write(...) on purpose.
      - linters:
          - errcheck
        path: _test\.go
